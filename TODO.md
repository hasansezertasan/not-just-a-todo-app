# TODO

- Add tests using pytest.
- Do coverage check, run tests across different versions using tox and test the UI with playwright.
- Daily Routine (automatically instantiated every day)
- Instant Tasks (tasks that are not in a sequence)
- Add a Dockerfile, publish to Docker hub.
